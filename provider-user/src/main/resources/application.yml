server:
  port: 8080
  servlet:
    context-path: /
spring:
  datasource:
    url: jdbc:mysql://192.168.3.90:3306/cdm_db
    driver-class-name: com.mysql.jdbc.Driver
    username: cdm
    password: cdm
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always